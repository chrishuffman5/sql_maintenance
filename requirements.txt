# DuckDB Export Tool - Python Dependencies

# Core database connectivity
# DuckDB 1.1.0+ required for sqlserver_scanner extension
duckdb>=1.1.0
pyodbc>=4.0.39
psycopg2-binary>=2.9.9
cx-Oracle>=8.3.0

# AWS S3 support (via DuckDB httpfs extension)
# Note: DuckDB's httpfs extension handles S3 connectivity

# Utilities
python-dateutil>=2.8.2
